@model BookModel
@{
    ViewData["Title"] = "Kitap Ekleme";

}

<div class="row">
    <div class="col">
        <h1 class="display-4 border-bottom text-center pb-2">@ViewData["Title"]</h1>
    </div>
</div>

<div class="row">
    <div class="col">
        <div class="mb-4">
            <div asp-validation-summary="All" class="text-danger small"></div>
        </div>
    </div>
</div>

<div class="col">
    <div>
        <h2>Kitap Bilgileri</h2>
        <hr />
    </div>

    <form method="post" asp-action="Index" enctype="multipart/form-data">

        <div class="text-center mb-3">
            <img src="~/uploads/No_Image.jpg" width="128" class="img-thumbnail" />
        </div>

        <div class="form-group mb-3">
            <label class="form-label small">Kitap Kapağı<span class="badge bg-danger ms-2">zorunlu</span></label>
            <input type="file" name="file" class="form-control" accept=".jpg" />
            <div class="text-secondary">
                <span class="small">
                    <i class="fa fa-info-circle me-2"></i>Sadece jpg formatında
                </span>
            </div>
        </div>

        <div class="form-group">
            <label class="form-label small">Kitap Adı<span class="badge bg-danger ms-2">zorunlu</span></label>
            <div class="input-group mb-3">
                <input name="bookname" type="text" class="form-control" />
            </div>
        </div>

        <div class="form-group">
            <label class="form-label small">Yazar<span class="badge bg-danger ms-2">zorunlu</span></label>
            <div class="input-group mb-3">
                <input name="author" type="text" class="form-control" />
            </div>
        </div>

        <div class="form-group">
            <label class="form-label small">Türü<span class="badge bg-danger ms-2">zorunlu</span></label>
            <div class="input-group mb-3">
                <input name="booktype" type="text" class="form-control" />
            </div>
        </div>

        <div class="form-group">
            <label class="form-label small">Özet<span class="badge bg-danger ms-2">zorunlu</span></label>
            <div class="input-group mb-3">
                <input name="booksubject" type="text" class="form-control" />
            </div>
        </div>

        <div class="form-group">
            <label class="form-label small">Stok<span class="badge bg-danger ms-2">zorunlu</span></label>
            <div class="input-group mb-3">
                <input name="stock" type="number" class="form-control" />
            </div>
        </div>

        <div class="form-group">
            <label class="form-label small">Yayın Tarihi<span class="badge bg-danger ms-2">zorunlu</span></label>
            <div class="input-group mb-3">
                <input name="publishingdate" type="date" class="form-control" />
            </div>
        </div>

        <button class="btn btn-secondary" type="submit"><i class="fa-solid fa-floppy-disk me-2"></i>Kaydet</button>

    </form>

</div>

@section Scripts{
    @if (ViewData["Result"] != null && ViewData["Result"] == "Ok")
    {
        <script>
            toastr["success"]("Kitap başarıyla eklenmiştir.", "Başarılı");
        </script>
    }
    }



